services:
  redis:
    image: redis:alpine
    ports:
    - "6379:6379"
  sibcom2:
    build: .
    command: "/sibcom2"
    links:
    - redis
    ports:
    -  8000:8000
    environment:
      REDIS_URL: "redis://redis"

